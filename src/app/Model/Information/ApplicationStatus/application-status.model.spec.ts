import { ApplicationStatus } from './application-statusmodel';

describe('ApplicationStatus', () => {
  it('should create an instance', () => {
    expect(new ApplicationStatus()).toBeTruthy();
  });
});
